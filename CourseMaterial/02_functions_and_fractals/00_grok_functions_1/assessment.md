1. What is the definition of a function:
    - Something that takes input(s) and produces an output
    - Something that takes a series of input(s) and adds them
    - Something that produces a random output unrelated to input(s)
    - Something(s) for which individuals are suited
2. In math, the `+` symbol can be considered a function. Why?
    - Because it takes input(s) and produces a single output
    - Because it has lower precedence than the `*` or `/` symbol
    - Because it combines any two numbers together
    - Because it is required to take one more more inputs
3. Why is `\sqrt(4) + 3` different from `\sqrt(4 + 4)`
    - Because the implied ordering of the functions is different
    - Because floats and integers can't be added together
    - Because you can take the square root of 4 and get a whole number
    - Because the `+` and `\sqrt` symbols result in different outputs
4. Match the following examples with the number of functions in them:
    - `3 - |-3| + 5`
    - `3 + 4`
    - `\sqrt(a**2 + b**2)`
    - `\sqrt(6 + 2)`
5. Match the following math equations with their CS representations:
    - `\sqrt(4) + 3`
    - `\sqrt(4 + 3)`
    - `\sqrt(4) + \sqrt(3)`
    - `\sqrt(4 + \sqrt(3))`
6. What would be a better "name" than `f` for the following function: `f(x) = x * x`:
    - divide(x)
    - add_one(x)
    - double(x)
    - square(x)
7. What would be a better "name" than `f` for the following function: `f(x, y) = x * y`:
    - area(x)
    - add_one(x)
    - double(x)
    - dog_years(x)
8. Assume `f(x) = \sqrt(x)` and `g(x) = x + 5`. Match the following function calls with their results:
    - `f(16)`
    - `g(3)`
    - `f(f((16))`
    - `g(g(4))`
    - `f(g(4))`
    - `g(f(4))`
