1. What is the definition of a function
    - Something that takes input(s) and produces an output
    - Something that takes a single input and produces multiple outputs
    - Something that produces a constant output unrelated to input(s)
    - Something that takes a set of input(s) and combines them
2. In math, the / symbol can be considered a function. Why?
    - Because it takes two inputs and produces a single output
    - Because it combines any two or more numbers together
    - Because it is required to take one or more inputs when "called"
    - Because it has higher precedence than the + or - symbol
3. Label each of the parts of the function that are pointed to in the picture below:
    - Name of function
    - Input(s) to function
    - Creates function
    - Function call
    - Output of function
4. What does the `return` statement do?
    - It gives the result of a function's computation to the calling code
    - It pauses the program to provide the total time a function ran for
    - It labels a particular variable as important within the body of a function
5. What is the result of running the following code: `add_one(add_one(double(add_one(3))))`
6. What is the result of running the following code: `add(mul(4, 3), add(mul(2, 2), add(1, 6)))`
7. Match the following functions with their "type annotation"
    - `add(x, y)` -> `int, int -> int`
    - `add_one(x)` -> `int -> int`
    - `greet(x)` -> `str -> str`
    - `area(x)` -> `int -> float`
    - `meet(x, y)` -> `str, str -> str`
    - `repeat(x)` -> `int -> str`
8. Match the following math functions with their Python equivalent
    - `f(x) = x^2`
    - `f(x, y) = x * y`
    - `f(x) = x * 2`
    - `f(x, y) = x + y`
9. Given the input of 5, what ordering of the functions below will produce the output 12:
    - `double(double(subtract_one(subtract_one(5))))`
    - `subtract_one(double(double(subtract_one(5))))`
    - `subtract_one(subtract_one(double(double(5))))`
    - `double(subtract_one(subtract_one(double(5))))`
10. A recipe can be thought of as a function. What are its inputs and outputs?
    - Input: ingredients, Output: a meal
    - Input: cooking time, Output: temperature
    - Input: recipe name, Output: ingredients
    - Input: chef skill level, Output: cut fingers