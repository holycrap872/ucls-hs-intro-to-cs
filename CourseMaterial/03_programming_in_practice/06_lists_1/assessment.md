0. What does the following bit of code print out?
    ```python
    x = "abc123"
    y = x[0] + x[2]
    print(y)
    ```
1. What does the following bit of code print out?
    ```python
    x = ["a", "b", "c", "1", "2", "3"]
    y = x[0] + x[2]
    print(y)
    ```
2. For the following bit of code, what is the value of the "accumulator variable" each time it goes through the loop?
    ```python
    x = ""
    for c in "hello":
        x = c + x + c
    ```
    - The first time through the loop, x is
    - The second time through the loop, x is
    - The third time through the loop, x is
    - The fourth time through the loop, x is
    - The fifth time through the loop, x is
3. For the following bit of code, what is the value of the "accumulator variable" each time it goes through the loop?
    ```python
    x = 1
    for i in range(4):
        x = x + i
    ```
    - The first time through the loop, x is
    - The second time through the loop, x is
    - The third time through the loop, x is
    - The fourth time through the loop, x is
4. What does the following bit of code print out?
    ```python
    l = [4, 5, 6, 7, 8]
    for i in range(5):
        l[i] = l[i] * 2
    print(l)
    ```
5. In what ways are strings the same as lists.
    - You can iterate through them
    - You can print them out
    - They are composed of individual elements
    - You can use an index to access their elements
    - You use quotes to create them
    - They can be empty
    - You can get their length
